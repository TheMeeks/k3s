apiVersion: v1
kind: Service
metadata:
  name: code-server
  namespace: servarr
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8443
  selector:
    app: code-server
  type: LoadBalancer
