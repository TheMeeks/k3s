apiVersion: v1
kind: PersistentVolume
metadata:
  name: general-pv
  labels:
    type: local
spec:
  storageClassName: general
  capacity:
    storage: 180Ti
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/unraid/general"
  persistentVolumeReclaimPolicy: Retain