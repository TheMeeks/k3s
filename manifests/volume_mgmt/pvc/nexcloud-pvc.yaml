apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-pvc
  namespace: nextcloud
spec:
  storageClassName: nextcloud
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 180Ti