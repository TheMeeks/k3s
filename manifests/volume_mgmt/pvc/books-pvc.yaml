apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: books-pvc
  namespace: servarr
spec:
  storageClassName: books
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 180Ti
